import { Andika, Exo } from 'next/font/google';

export const andika = Andika({
    weight: ['400', '700'],
    subsets: ['latin', 'latin-ext'],
});

export const exo = Exo({
    weight: ['400', '700'],
    subsets: ['latin', 'latin-ext'],
});